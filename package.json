{
  "name": "epic-gardening",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && vite build && echo 'Build completed'"
  },
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
