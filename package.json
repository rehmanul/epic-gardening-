{
  "name": "epic-gardening",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build",  # ACTUAL build command
    "preview": "vite preview"  # ACTUAL preview command
  },
  "engines": {
    "node": "20.x"  # Hard lock Node version
  }
}
