{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "start": "ng serve",
    "test": "ng test",
    "build": "ng build --prod"
  },
  "dependencies": {
    "@angular/core": "^12.0.0",
    "@angular/cli": "^12.0.0"
  },
  "devDependencies": {
    "@angular/compiler-cli": "^12.0.0"
  }
}
