{
  "name": "epic-gardening",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && vite build && echo 'Build completed'"
  },
  "engines": {
    "node": "16.x",
    "npm": "7.x"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
