{
  "name": "epic-gardening",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "your-build-tool-command", # e.g., "vite build", "webpack", etc.
    "start": "your-start-command" # e.g., "vite preview"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    // Your actual dependencies
  },
  "devDependencies": {
    // Your actual dev dependencies
  }
}
